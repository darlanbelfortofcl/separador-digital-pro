# Redis connection (optional). If empty or unreachable, the app falls back to synchronous mode.
REDIS_URL=redis://redis:6379/0

# Cleanup interval (minutes) for temp files
CLEANUP_INTERVAL_MINUTES=30

# Flask
SECRET_KEY=change-me
FLASK_DEBUG=0
HOST=0.0.0.0
PORT=5000
